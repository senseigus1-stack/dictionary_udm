Ӟечбур установлен.

Проверка состояния:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl -n {{ .Release.Namespace }} get jobs -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if .Values.ingress.enabled }}
Приложение: {{ if .Values.ingress.tls.enabled }}https{{ else }}http{{ end }}://{{ .Values.ingress.host }}
{{- else }}
Локальный доступ:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "zechbur.fullname" . }} 8000:{{ .Values.service.port }}
{{- end }}

Перед production-запуском задайте secrets.existingSecret или значения Telegram-секретов.
